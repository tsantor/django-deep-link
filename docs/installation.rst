============
Installation
============

At the command line::

    $ easy_install django-deep-link

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-deep-link
    $ pip install django-deep-link
